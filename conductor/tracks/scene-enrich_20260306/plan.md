# Implementation Plan: Scene Enrichment — Ground Plane, Skybox & Camera Cylinder

**Track ID:** scene-enrich_20260306
**Spec:** [spec.md](./spec.md)
**Created:** 2026-03-06
**Status:** [x] Complete

## Overview

Add three visual elements to the demo scene: a sky-colored background, a ground plane beneath the meshes, and a cylinder primitive that tracks the orbiting camera. All new geometry uses the existing Phong lighting pipeline (Phase 15). Implementation follows strict TDD.

---

## Phase 1: Sky Color & Ground Plane

### Tasks

- [x] Task 1.1: Write tests for `NewPlane()` primitive — verifies 4 vertices, 6 indices (2 triangles), all normals pointing +Y, configurable size. Commit `test:`.
- [x] Task 1.2: Implement `NewPlane(width, depth float64)` in `pkg/geometry/primitives.go`. Each vertex gets a grey color and +Y normal. Commit `feat: add ground plane primitive`.
- [x] Task 1.3: Change the GPU clear color from dark grey to light blue (e.g. R=0.53 G=0.81 B=0.98). Commit `feat: set sky-blue background clear color`.
- [x] Task 1.4: Add the ground plane to `buildDemoScene()` in `cmd/renderer-rt/main.go` at Y = -1, scale ~10. Commit `feat: add ground plane to demo scene`.

### Verification

- [ ] Ground plane renders beneath meshes with correct Phong shading.
- [ ] Background is light blue.
- [ ] All existing tests pass.

---

## Phase 2: Cylinder Primitive

### Tasks

- [x] Task 2.1: Write tests for `NewCylinder(segments int)` — vertex count, index count, lateral normals are radial+unit-length, cap normals are axis-aligned. Commit `test:`.
- [x] Task 2.2: Implement `NewCylinder(segments int)` with lateral quad faces + top/bottom caps. Height 1 (Y from -0.5 to 0.5), radius 0.5. Per-face normals for flat shading. Commit `feat: add cylinder primitive`.

### Verification

- [ ] Cylinder renders correctly with Phong shading.
- [ ] All geometry tests pass.

---

## Phase 3: Camera Cylinder & Integration

### Tasks

- [x] Task 3.1: Position a small cylinder at the camera location in the demo scene, oriented along the camera's forward direction. Update its transform each frame. Commit `feat: attach cylinder to orbiting camera`.
- [x] Task 3.2: Run full quality gates (`go fmt`, `go vet`, `golangci-lint`, `go test ./...`). Commit `chore: mark scene-enrich track complete`.

### Verification

- [ ] All acceptance criteria in spec.md met.
- [ ] PR merged to main, CI green.

---

_Generated by Conductor. Tasks marked [~] when in progress, [x] when complete._
