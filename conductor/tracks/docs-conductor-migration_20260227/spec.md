# Specification: Migrate .claude/ Context into Conductor Artifacts

**Track ID:** docs-conductor-migration_20260227
**Type:** Chore
**Created:** 2026-02-27
**Status:** Draft

## Summary

Migrate all content from `.claude/docs/` (16 files) and `.claude/tasks/` (7 files) into Conductor-compatible artifacts, establish a clear policy on what belongs in `conductor/` vs. CLAUDE.md, and update CLAUDE.md to reference Conductor as the canonical context source.

## Context

Phases 0–8 are complete. The project has 16 docs covering architecture, component specs, roadmap, test strategy, CI/CD, and 3 GPU-roadmap docs (Phases 9–16). Seven task summaries document completed phases. These were written for Claude Code context injection (loaded via CLAUDE.md) rather than Conductor's structured model, creating duplication and no single source of truth.

## Motivation

The existing docs are project-specific markdown files written for Claude Code context injection, not structured for Conductor's product/tech-stack/workflow/tracks model. Migrating them enables `/conductor:implement`, `/conductor:status`, and track-based planning to work correctly with accurate context.

## Acceptance Criteria

- [ ] `conductor/product.md` and `product-guidelines.md` fully supersede `01-mvp-vision.md` and CLAUDE.md's "Project Overview" section.
- [ ] `conductor/tech-stack.md` fully supersedes the tech details scattered across `02-architecture-overview.md`, `13-cicd-infrastructure.md`, and CLAUDE.md's "Development Commands" / "Code Quality" sections.
- [ ] `conductor/workflow.md` fully supersedes the TDD, Git, and PR workflow sections of CLAUDE.md.
- [ ] `conductor/architecture.md` (new) consolidates `02-architecture-overview.md`, coordinate system, pipeline data-flow, and package dependency rules.
- [ ] Completed phases (0–8) are represented as archived Conductor tracks, each with a minimal spec + plan + metadata referencing the original task summary.
- [ ] GPU roadmap phases (9–16) are represented as pending Conductor tracks, each with spec and plan derived from `12-development-roadmap.md` and relevant GPU docs (`14`, `15`, `16`).
- [ ] All `.claude/tasks/` files are preserved in-place as historical records (not deleted); `conductor/tracks/` links to them where relevant.
- [ ] CLAUDE.md is trimmed to: project overview (1 paragraph), dev commands, and pointers to `conductor/` for all other context.
- [ ] No significant information is lost (verified by `conductor/migration-audit.md` review).

## Dependencies

- Conductor setup complete (`conductor/product.md` etc. exist) ✅
- All existing `.claude/docs/` and `.claude/tasks/` files readable ✅

## Out of Scope

- Implementing any GPU phase code (Phases 9–16).
- Changing `.claude/docs/` content (files are preserved as-is).
- Deleting `.claude/tasks/` historical files.
- Modifying CI/CD pipeline or linter config.

## Technical Notes

Migration strategy:
1. Audit each `.claude/docs/` file → map to target Conductor artifact.
2. For component specs (03–09), consolidate into `conductor/architecture.md` (single reference doc) rather than 7 separate files.
3. For the roadmap (`12-development-roadmap.md`), each phase becomes a Conductor track with appropriate status (archived vs. pending).
4. CLAUDE.md retains only what Conductor cannot express: dev commands, gotchas, and external resource links.

---

_Generated by Conductor. Review and edit as needed._
