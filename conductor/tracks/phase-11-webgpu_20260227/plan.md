# Implementation Plan: Phase 11 — WebGPU Integration

**Track ID:** phase-11-webgpu_20260227
**Spec:** [spec.md](./spec.md)
**Created:** 2026-02-27
**Status:** [ ] Not Started

## Overview

Integrate wgpu-native via CGo into `GPUBackend`. Milestone: a coloured triangle rendered by the GPU appears in the GLFW window. CPU backend continues to work unchanged.

---

## Phase 1: wgpu-native CGo Bindings

Set up CGo wrapper and confirm the GPU device initialises without errors.

### Tasks

- [ ] Task 1.1: Download wgpu-native pre-built binaries for target platform(s); document in `README.md`. Commit `docs: add wgpu-native setup instructions`.
- [ ] Task 1.2: Create `pkg/gpu/wgpu.go` with CGo `#include "wgpu.h"` and `#cgo LDFLAGS`; write test that creates `wgpu.Instance` and immediately drops it (smoke test). Commit `test:` then `feat: add wgpu-native CGo bindings`.
- [ ] Task 1.3: In `GPUBackend.Init`: request adapter (sync), create device, create surface from GLFW window handle, create swapchain. Commit `feat: init wgpu device and swapchain`.

### Verification

- [ ] `--backend gpu` opens a window without crash.
- [ ] GPU device is created on D3D12 (Windows) / Metal (macOS) / Vulkan (Linux).

---

## Phase 2: Render Pass & Hello Triangle

### Tasks

- [ ] Task 2.1: Write hardcoded WGSL vertex + fragment shader (coloured triangle). Commit `feat: add hello triangle WGSL shaders`.
- [ ] Task 2.2: Implement `GPUBackend.RenderFrame`: acquire swapchain texture → begin render pass → create pipeline → draw(3, 1, 0, 0) → end pass → present. Commit `feat: render hello triangle via wgpu`.
- [ ] Task 2.3: Verify triangle visible at 60 fps. Commit `feat: confirm GPU hello triangle at 60fps`.

### Verification

- [ ] Coloured triangle visible in window using GPU path.
- [ ] CPU backend still works with `--backend cpu`.

---

## Phase 3: Cleanup & Documentation

### Tasks

- [ ] Task 3.1: Gate `pkg/gpu` tests behind `//go:build gpu` tag; add `Makefile` target `make test-gpu`. Commit `chore: gate GPU tests behind build tag`.
- [ ] Task 3.2: Update `README.md` with GPU backend section. Commit `docs:`.
- [ ] Task 3.3: Run `go test ./...` (non-GPU tests pass on CI); `go test -tags=gpu ./pkg/gpu/...` passes locally. Commit `chore: mark Phase 11 complete`.

### Verification

- [ ] All acceptance criteria in `spec.md` met.
- [ ] PR merged to `main`, CI green.

---

_Generated by Conductor. Tasks marked [~] when in progress, [x] when complete._
